import 'package:flutter/material.dart';

class LocaleSwitchPage extends StatelessWidget {
  const LocaleSwitchPage({super.key});

  @override
  Widget build(BuildContext context) => Scaffold(
        appBar: AppBar(title: const Text('SettingLocale')),
      );
}
